// Check given number is available  in range 100–200 or not. (only using ternary operator).

let number = 150;
let result = (number >= 100 && number <= 200) ? "Number is in the range 100-200" : "Number is out of the range 100-200";
console.log(result);
// Given payment method, print "Card payment" if method is "card", else "Cash payment".

let paymentMethod = "card";
let result = (paymentMethod === "card") ? "Card payment" : "Cash payment";
console.log(result);
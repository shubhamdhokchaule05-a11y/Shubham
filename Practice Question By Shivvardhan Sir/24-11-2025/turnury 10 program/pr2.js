// Given a password length, use a ternary to print "Strong password" if length > 8, else "Weak password".
 
let password = 5;
let result = (password > 8) ? "Strong password" : "Weak password";
console.log(result);
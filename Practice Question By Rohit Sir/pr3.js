// Use the substring() method to extract the first four characters of the string.
// If the extracted part is exactly "Java", print "OK".
// Otherwise, print "NOT OK".

let a = "Java";

if(a.substring(0,4)==="Java"){
    console.log(a,"It is OK");
}

else{
    console.log(a,"It is Not OK");
    
}